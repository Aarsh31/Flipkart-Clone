# Copy the backend values below into server/.env before running the app.

DATABASE_URL="your-neon-pooled-connection-string"
DIRECT_URL="your-neon-direct-connection-string"
PORT=4000
CLIENT_URL="http://localhost:5173"
DEFAULT_USER_EMAIL="demo@flipkartclone.local"
JWT_SECRET="replace-with-a-long-random-secret"
MAIL_FROM="Flipkart Clone <no-reply@example.com>"
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
